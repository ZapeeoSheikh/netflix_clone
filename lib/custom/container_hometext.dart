import 'package:flutter/material.dart';

class ContainerHomeText extends StatelessWidget {
  const ContainerHomeText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
